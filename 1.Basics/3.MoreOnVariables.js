// 1. We can`t define a variable more than once
let petName = 'Puff';
petName = 'Spark';
console.log(petName);

// 2. Thre are rules related to variable names
// Variable names can start with _, $, A - Z, a - z
// Variable names cannot be reserved keywords
let a1 = 45;
let $one = 1;
let _one = 1;